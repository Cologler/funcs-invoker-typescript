{
  "name": "event-invoker",
  "version": "0.1.0",
  "description": "",
  "main": "./dist/event-invoker.js",
  "jsdelivr": "./dist/event-invoker.browser.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "tsc",
    "browserify": "tsc && browserify . -s event-invoker -o dist/event-invoker.browser.js"
  },
  "author": "Cologler",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^16.2.2"
  }
}
